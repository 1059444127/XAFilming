<Window x:Class="UIH.XA.Filming.ViewMock.JobManagerWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
            <DataGrid ItemsSource="{Binding JobColletion}" AutoGenerateColumns="True">
                <DataGridTextColumn Header="Index" Binding="{Binding ID}"></DataGridTextColumn>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}"></DataGridTextColumn>
                <DataGridTextColumn Header="Progress" Binding="{Binding Progress}"></DataGridTextColumn>
        </DataGrid>
    </Grid>
</Window>
